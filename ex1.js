/* Crie um afunção chamada calcularPreçoComDesconto, essa função vai receber 3 parâmetros - (valor do produto,
quantidade e uma função callback que aplica o desconto e devolve o valor atualizado)
*/

import PromptSync from "prompt-sync";
const prompt = PromptSync()

function calcularPreçoComDesconto(a, b, c, callback) {
    
}